import { PortalClient } from "@/components/portal/portal-client";

export const metadata = {
  title: "Client Portal — Sweeney Nutrition",
};

export default function PortalPage() {
  return <PortalClient />;
}
